COMMAND: deploy

Do:
- Provide Dockerfiles for api/web
- docker-compose (api, web, db, redis)
- GitHub Actions CI (lint, test, build)
- Hosting plan (VPS or cloud)

Output:
- All config files
- Steps to run in prod
